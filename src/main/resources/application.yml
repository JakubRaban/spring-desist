spring:
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/desistdb?autoReconnect=true&allowPublicKeyRetrieval=true
    username: desistdb
    password: desistdb
    driver-class-name: com.mysql.cj.jdbc.Driver